<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= title
    body
        h1
            a(href="/topic") programing language learning list
        ul
            each topic in topics
                li
                        a(href=`/topic/${topic}`)= topic
        h2= title
        p= content
    div
        a(href="/form") new